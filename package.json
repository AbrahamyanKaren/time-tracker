{
  "name": "time-tracker",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "browser-sync start --server --files 'src/**/*.*'",
    "compile:scss": "sass --watch assets/styles:css --style=compressed --no-source-map && serve"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "sass": "^1.77.8"
  },
  "dependencies": {
    "browser-sync": "^3.0.2"
  }
}
